# Given an array of integers (0 and negative included) return the
# largest sum of non adjacent numbers

# Do it on O(n) and constant space O(1)

from random import randint

nums = [randint(-20, 20) for _ in range(10)]

print(nums)

test = [1, 2, 3]


def largest_non_adjacent_sum(nums):
    # for num in enumerate(nums):
    pass

